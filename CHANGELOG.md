# 更新记录

## 0.5.0 — 2026-09-25

- 根据用户在大秘境中的失败反馈，删除全部焦点功能：焦点列、本机焦点读取、聊天通报解析和缓存、自定义接收格式及相关设置入口。
- 删除 `Communication.lua` 与 `/ptw formats`，小地图保留左键设置入口，设置面板收紧布局。
- 保留队友当前目标、团队标记、背景透明度、场景筛选、位置、大小和预览功能。
- 载入时清除四个退役焦点配置字段，保留其他偏好；开发安装工具先备份再清理旧通信模块。
- 27 个 Lua 场景与 7 项交付检查通过，1 项符号链接检查因本机权限跳过。本版未在真实游戏中验证，CurseForge 仍未提交或发布。

## 0.4.3 — 2026-09-25

- 默认接收格式仅保留 `我打断%mark` 和 `我的焦点打断是 {rt%mark} %name` 两条；新安装可直接接收对应的 SeUI 名称与标记通报，其他格式仍可自定义。
- 新安装、无效配置回退和恢复内置格式使用这两条；升级保留已保存的模板及空列表，不自动替换或覆盖。

本版仅调整默认格式，离线检查见 `validation/TESTING.md`；小地图按钮和原生设置入口仍待实机确认。CurseForge 资料尚未提交或发布。

## 0.4.2 — 2026-09-25

- 新增小地图按钮：左键打开设置，右键编辑接收格式；无需第三方库，可供 EUI 小地图按钮收纳识别。
- 注册原生“设置 → 插件 → PartyTargetWatch 队友目标”分类，提供进入现有设置与接收格式面板的按钮。旧版缺少这两个入口，与游戏限制或 CurseForge 收录无关。
- 新增 `Integration.lua`，发行 ZIP 由 7 个文件增至 8 个；小地图收纳与原生设置入口均待 `/reload` 后实机确认。

- 删除插件间焦点同步，包括设置开关、前缀注册、消息发送与接收、握手、保活和远端缓存；不再处理旧焦点协议。
- 保留当前目标监控、聊天通报接收，以及本机公开焦点的只读显示；本插件不发送普通聊天。
- “焦点与通报”仅保留“显示焦点 / 通报列”和“接收队友的焦点通报”两个开关。接收关闭显示“未开启接收”，开启且无可用记录显示“等待通报”。
- 升级清除旧 `shareFocus` 字段，其余开关、模板、位置与透明度等设置保留。接收格式、名称和标记解析、300 秒有效期及“取消打断”保持不变。

本版尚待 `/reload` 与实机确认，离线检查详见 `validation/TESTING.md`。CurseForge 资料为 prepared，尚未提交或发布。下列旧版本的同步说明仅为历史记录。

## 0.4.1 — 2026-09-25

- 将设置分区改为“焦点与通报”，依次展示“显示焦点 / 通报列”“接收队友的焦点通报”和“插件间焦点同步（可选）”；保留已保存的开关状态。
- 将格式入口改为“接收格式…”，说明聊天方案只需开启前两项，发送方无需安装本插件；SeUI 发送端使用 `%f`，接收模板使用 `%name`。
- 主窗口列名改为“焦点 / 通报”，底部注明“黄色：队友最近一次通报”；接收开启、同步关闭且无记录时显示“等待通报”。黄色记录有效 5 分钟，换焦点后需重新通报，队伍消息“取消打断”清除本人记录。
- 保留可选同步：双方安装并启用，且游戏允许读取与通信时生效。聊天解析、记录有效期与同步协议不变。

本版为界面与说明调整，新界面待 `/reload` 后实机确认；0.4.0 的名称、图标与 SeUI 自定义格式成功反馈保留为历史验证。CurseForge 资料为 prepared，尚未提交或发布。

## 0.4.0 — 2026-09-25

- 新增 `%name` 聊天名称占位符，支持名称声明、标记声明及二者组合；声明包含名称时优先显示黄色名称，仍表示队友当时的声明，不验证实时焦点。
- 名称前置 `{rtN}` 按标记图标提取，避免在名称里留下 `rtN`；前缀与显式 `%mark` 冲突时拒绝声明，不读取单位的受保护标记。
- 每条模板允许最多一个 `%name`、一个 `%mark`，至少包含其中之一；最多两个 `%text`。只有名称的模板必须有固定文字，名称与通配文字不得相邻；清理后的名称最多 96 字节，未展开的 `%f`、`%t` 不作为名称接受。
- 新增默认模板 `PTW焦点：%name`；保留已有保存格式。“加入名称格式”按钮只把该模板加入编辑草稿，点击保存后才生效。
- 提供由玩家手动按下的原生宏示例：`/stopmacro [@focus,noexists]` 后接 `/p PTW焦点：%f`。插件不恢复主动普通聊天发送、通报按钮或快捷键。
- 保留公开值检查、完整消息匹配、歧义拒绝、当前组员校验及声明过期；受保护聊天仍不解析。只含标记的旧模板继续显示标记约定，不反查怪物名称。

76 个 Lua mock 场景通过（39 UI、37 通信），交付测试 7 通过、1 因 Windows 符号链接权限跳过。用户已现场确认原生 `%f` 展开和本版接收、显示名称；截图暴露的前置标记文字已修正，用户第二轮确认图标与纯怪物名称均正常显示。跨客户端送达、完整两行宏及战斗/大秘境效果未验证。CurseForge 资料为 prepared，尚未提交或发布。

## 0.3.1 — 2026-09-25

- 删除读取目标后主动发送普通聊天通报的功能，包括按钮、成员行点击、相关命令、快捷键与专用诊断。
- 保留目标与标记监控、自定义聊天声明模板、背景不透明度，以及默认关闭的公开焦点共享。聊天声明仅识别游戏允许读取的已发送消息，不代表实时真实焦点。
- 发行包移除 `Bindings.xml`；本地安装器在完整校验旧版备份后移除这个已废弃文件，保留其他插件和 SavedVariables。

64 个 Lua mock 场景通过（36 UI、28 通信），交付测试 7 项通过、1 项因 Windows 符号链接权限跳过。本版尚未实机验收。用户反馈的 0.3.0 追随者场景提示为“目标信息受保护”；本版移除该发送路径，不声称解除游戏限制。CurseForge 资料已更新为 0.3.1，仍为 prepared，尚未提交或发布。

## 0.3.0 — 2026-09-25

- 新增多行聊天声明模板编辑器：从设置中的“通报格式…”或 `/ptw formats` 打开；最多 20 条、每条 255 字节、总输入 8192 字节。
- 模板必须恰含一个 `%mark`，可含最多两个非空 `%text`；其余内容按字面整句匹配，按有效模板顺序识别，拒绝未知占位符及单模板内不同标记的歧义。
- 默认模板为 `我打断%mark` 与 `我的焦点打断是 {rt%mark}`，支持中文标记名、数字和 `{rtN}`。草稿可本地测试，不发送消息；保存才生效并清空旧声明，已保存模板支持重载保留。空列表停止新声明识别，固定取消语句仍有效。
- 新增背景不透明度 `0`–`1`，默认 `0.88`；仅影响背景、边框和行底色，文字与图标保持可见。
- 区分聊天锁定、目标信息受保护和成员信息受保护的通报失败提示；新增 `/ptw status` 本地公开诊断，不输出真实名称或发送消息。
- 限制事件只对 Chat 类型设置临时聊天锁定，修复无关事件造成的短暂误拦截；保留游戏实际聊天及秘密值限制。

69 个 Lua mock 场景通过（37 UI、32 通信）；交付测试 7 项通过、1 项因 Windows 符号链接权限跳过。新增功能尚未在游戏中加载验证，安装文件更新不等于实机通过。当前追随者场景的限制来源尚无 API 诊断，不能宣称已修复所有追随者通报限制。CurseForge 资料已更新为 0.3.0，仍为 prepared，尚未提交或发布。

## 0.2.0 — 2026-09-25

- 新增目标团队标记、按场景显示筛选，以及按钮、成员行、`/ptw announce` 和用户自行绑定按键的目标通报。
- 新增独立设置模块与焦点列；“显示焦点列”“焦点共享”“聊天打断声明记录”三个开关默认关闭。
- 可选共享各自公开可读的焦点，按当前组员身份校验、节流和握手；12 秒过期，离组清除，不保存接收数据。
- 可选严格识别“我打断”加八种标记及“取消打断”，声明有效期 300 秒；显示为黄色“约定”，真实可读焦点优先。
- 聊天锁定隐藏自动焦点缓存；秘密值不进入通信，不通过普通聊天或外部服务绕过大秘境等限制。
- 发行包新增 `Communication.lua`、`Settings.lua` 和 `Bindings.xml`，打包工具支持 XML 校验。

本版仅完成离线 mock 与交付验证，新增功能尚未实机验证。下方 0.1.0 的单人实测为历史记录，不代表本版已通过实机验收。

## 0.1.0 — 2026-09-25

首个版本，TOC 接口版本为 `120100`。

- 显示自己、小队与最多 40 名团队成员当前选中的目标。
- 显示离线、无目标或不可见状态，处理 API 读取失败及受限制的名称值。
- 提供简体中文监控窗口、两列团队布局与示例预览。
- 支持拖动、锁定、60%–200% 缩放、位置输入、居中、隐藏和恢复默认。
- 通过 SavedVariables 保存窗口位置及显示设置。
- 提供 `/ptw` 与 `/partytargetwatch` 命令。
- 添加 MIT 许可证、TOC 校验打包工具，以及保留备份和其他插件 SHA256 校验记录的本地安装工具。

已完成单人基础实机验证：`/reload` 后加载、打开独立设置窗口、切换预览、100% → 110% → 100% 缩放，以及自身当前目标名称实时更新。游戏内设置持久化、多人小队、团本和战斗仍待验证；尚未完成完整错误排查。
